[package]
name = "broken-app"
version = "0.1.0"
edition = "2024"

[dependencies]

[dev-dependencies]
criterion = "0.5"
rand = "0.10.0"

[[bench]]
name = "baseline"
harness = false

[profile.release]
debug = 1
